import React from 'react';
import styled from 'styled-components'

const HeaderStyle = styled.h1`
    text-transform: uppercase;
`

const Header = () => {
    return (
        <HeaderStyle> Coin Tracker </HeaderStyle>
)}

export default Header